PEERS=( $HOSTNAME peer-$HOSTNAME )
# PEERS=( zsync@$HOSTNAME zsync@peer-$HOSTNAME )

INCLUDES+=(
    /etc/hosts
    /etc/profile.d/ztools.sh
    /etc/ztools
    /opt/ztools
    # /etc/sudoers.d/zsync
    # /home/zsync
)

EXCLUDES+=(
    '*/.*.swp'
    '*/*~'

)

